{
  "name": "create-stk",
  "version": "0.1.3",
  "description": "Opinionated, unified project scaffolding CLI.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "type": "module",
  "bin": {
    "create-stk": "dist/index.js"
  },
  "scripts": {
    "cli:dev": "tsx src/index.ts",
    "test": "vitest run",
    "build": "tsup src/index.ts --format esm,cjs --dts",
    "lint": "tsc",
    "ci": "bun run lint && bun run test && bun run build",
    "release": "bun run lint && bun run test && bun run build && bun publish --access public",
    "prepare": "bun run build"
  },
  "files": [
    "dist",
    "assets",
    "README.md"
  ],
  "keywords": [
    "scaffold",
    "cli"
  ],
  "author": "Sphe",
  "license": "MIT",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "dependencies": {
    "@clack/prompts": "^1.4.0",
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "execa": "^9.6.1",
    "giget": "^3.2.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/node": "^25.9.1",
    "tsup": "^8.5.1",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
