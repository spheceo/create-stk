{
  "name": "create-stk-repo",
  "private": true,
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --filter=./apps/* --filter=!./apps/cli",
    "cli:dev": "turbo run cli:dev --filter=./apps/cli",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "ci": "turbo run lint && turbo run test && turbo run build",
    "release": "turbo run lint test build && changeset publish",
    "publish": "bun publish --cwd apps/cli --access public"
  },
  "trustedDependencies": [
    "esbuild"
  ],
  "overrides": {
    "picomatch": "4.0.4",
    "postcss": "8.5.14",
    "rollup": "4.60.3",
    "vite": "7.3.2"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "turbo": "^2.9.9"
  }
}
